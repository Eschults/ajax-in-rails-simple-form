<%= simple_form_for [ restaurant, review ], remote: true, data: { action: "ajax:error->ajax-form#renderForm ajax:success->ajax-form#renderForm"} do |f| %>
  <%= f.input :content, label: "Add yours" %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
