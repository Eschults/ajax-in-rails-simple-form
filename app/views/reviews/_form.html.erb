<%= simple_form_for [ restaurant, review ], remote: true, data: { action: "ajax:error->ajax-form#renderPartial ajax:success->ajax-form#renderPartial"} do |f| %>
  <%= f.input :content, label: "Add yours" %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
